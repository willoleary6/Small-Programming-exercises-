import java.util.Scanner;
public class Pattern
{
 public static void main(String [] args)
 {
  int x, y;
  String ErrorMessage = "Error restart and try again", result = "";
  Scanner in = new Scanner(System.in);
  System.out.println("Enter a number between 5 and 9 ");
  x = Integer.parseInt(in.nextLine());
  if ( x < 5 || x > 9) result = ErrorMessage;
  else 
	  for (y = x; y >= 1; y = y -1)
	  {
		 for  
		
	  }
	 
 }
}